{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main North Vector window",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
